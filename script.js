// Define global variables or constants here

// Function to fetch and display upcoming events
function fetchEvents() {
    // Use fetch or XMLHttpRequest to get event data from the server
    // Update the events section with the fetched data
}

// Function to handle event registration
function registerForEvent(eventId) {
    // Use fetch or XMLHttpRequest to send registration data to the server
    // Display a success or error message based on the server response
}
function showAlert() {
    alert('Hello, World!');
}
// Add event listeners or other initialization code here

// Fetch events when the page loads
document.addEventListener('DOMContentLoaded', fetchEvents);
